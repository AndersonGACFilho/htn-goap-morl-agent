\begin{figure}[htbp]
\centering
\resizebox{0.94\textwidth}{!}{%
\begin{tikzpicture}[
    font=\small,
    >=Latex,
    node distance=9mm and 22mm,
    block/.style={
        rectangle, rounded corners=2mm, draw, thick, align=center,
        minimum width=36mm, minimum height=10mm
    },
    offline/.style={block, draw=morlred, fill=morlred!8},
    online/.style={block, draw=htnblue, fill=htnblue!8},
    arrow/.style={->, thick}
]
    \node[font=\bfseries\large,text=morlred] (offlineTitle)
        {PRE-DEPLOYMENT --- Training};
    \node[offline, below=7mm of offlineTitle] (episodes)
        {Sample episodes\\$(C,WS,\mathbf{w})$};
    \node[offline, below=of episodes] (explore)
        {Explore methods\\and decompositions};
    \node[offline, below=of explore] (simulate)
        {Execute / simulate\\primitive behavior};
    \node[offline, below=of simulate] (reward)
        {Vector rewards\\$\mathbf{r}_t$};
    \node[offline, below=of reward] (returns)
        {Method return\\$\mathbf{R}_M=\sum_{i=0}^{k-1}\gamma^i\mathbf{r}_{t+i}$};
    \node[offline, below=of returns] (learn)
        {Learn $\mathbf{Q}_\theta(C,WS,M,\mathbf{w})$};
    \draw[arrow,morlred] (episodes) -- (explore);
    \draw[arrow,morlred] (explore) -- (simulate);
    \draw[arrow,morlred] (simulate) -- (reward);
    \draw[arrow,morlred] (reward) -- (returns);
    \draw[arrow,morlred] (returns) -- (learn);

    \node[font=\bfseries\large,text=htnblue,right=58mm of offlineTitle]
        (onlineTitle) {ONLINE --- Runtime};
    \node[online, below=7mm of onlineTitle] (input)
        {$C_t,\ WS_t,\ \mathbf{w}_t$};
    \node[online, below=of input] (mask)
        {HTN feasible-method mask};
    \node[online, below=of mask] (infer)
        {One MORL inference};
    \node[online, below=of infer] (argmax)
        {$\displaystyle
        \begin{gathered}
        M_t^*=\arg\max_{M_i}\\[-1mm]
        U_{\mathbf{w}_t}(\mathbf{Q}_\theta)
        \end{gathered}$};
    \node[online, below=of argmax] (decompose)
        {Decompose only $M_t^*$};
    \node[online, below=of decompose] (next)
        {Execute / next\\HTN decision point};
    \draw[arrow,htnblue] (input) -- (mask);
    \draw[arrow,htnblue] (mask) -- (infer);
    \draw[arrow,htnblue] (infer) -- (argmax);
    \draw[arrow,htnblue] (argmax) -- (decompose);
    \draw[arrow,htnblue] (decompose) -- (next);
    \draw[arrow,dashed] (learn.east) -- ++(16mm,0) |-
        node[pos=.25,above,align=center] {learned parameters\\$\theta$} (infer.west);

    \node[below=10mm of learn,align=center,text=morlred]
        {Higher computational cost\\accepted during training};
    \node[below=10mm of next,align=center,text=htnblue]
        {Low-latency hierarchical\\decision during gameplay};
\end{tikzpicture}%
}
\caption{Separação entre treinamento pré-implantação e decisão online. O treinamento
explora consequências de métodos e aprende estimativas vetoriais; em execução,
uma inferência mascarada seleciona um único método HTN viável sem expandir todas
as decomposições candidatas.}
\label{fig:offline-online-morl}
\end{figure}
